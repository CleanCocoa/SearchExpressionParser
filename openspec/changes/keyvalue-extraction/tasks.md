## 1. Key-Value Extraction Function

- [ ] 1.1 Write tests for `keyValueNodes(in:)` with single `.keyValue` leaf
- [ ] 1.2 Write tests for `keyValueNodes(in:)` with non-key-value expressions returning `[]`
- [ ] 1.3 Write tests for `keyValueNodes(in:)` traversing `.and`, `.or`, `.not` composites
- [ ] 1.4 Write tests for `keyValueNodes(in:)` preserving duplicates
- [ ] 1.5 Write tests for `keyValueNodes(in:)` with deeply nested trees
- [ ] 1.6 Implement `keyValueNodes(in:)` function (using internal fold or iterative traversal)
