## 1. Key-Value Extraction Function

- [x] 1.1 Write tests for `keyValueNodes(in:)` with single `.keyValue` leaf
- [x] 1.2 Write tests for `keyValueNodes(in:)` with non-key-value expressions returning `[]`
- [x] 1.3 Write tests for `keyValueNodes(in:)` traversing `.and`, `.or`, `.not` composites
- [x] 1.4 Write tests for `keyValueNodes(in:)` preserving duplicates
- [x] 1.5 Write tests for `keyValueNodes(in:)` with deeply nested trees
- [x] 1.6 Implement `keyValueNodes(in:)` function (using internal fold or iterative traversal)
