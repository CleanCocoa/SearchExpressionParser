## 1. PhraseExtractor

- [x] 1.1 Write tests for `PhraseExtractor` leaf methods: `evaluateContains` returns `[string]`, `evaluateKeyValue` returns `[]`, `evaluateAnything` returns `[]`
- [x] 1.2 Write tests for `PhraseExtractor` combinator methods: `evaluateNot` returns `[]`, `evaluateAnd` concatenates, `evaluateOr` concatenates
- [x] 1.3 Implement `PhraseExtractor` struct
- [x] 1.4 Write integration tests: full tree extraction via `evaluate(_:with:)` including post-normalize usage
